# Data — too large for git (root-level only, not src/forensic_mh/data)
/data/
/results/

# Python
__pycache__/
*.pyc
.venv/
.uv/
*.egg-info/
dist/
build/

# Notebook checkpoints
.ipynb_checkpoints/

# IDE
.vscode/
.idea/

# Stray tabix/bcf indexes from remote extraction (land in cwd)
*.tbi
*.csi
hgdp_wgs.*.vcf.gz*
hgdp1kgp_chr*.bcf*

# Build / compile caches (land in cwd)
catboost_info/
torchinductor_*/
.torchinductor/

# Course assignment / paperwork artifacts (not for the public repo)
*.docx

# Secrets — never commit (public repo)
.env
*.env
.env.*

# System
.DS_Store

# Claude Code project cache (user-specific)
.claude/
